


/* // old pause
mouse_hover = collision_point(device_mouse_x_to_gui(0), device_mouse_y_to_gui(0), id, true, false);

if (mouse_hover) {
	
	image_blend = merge_color(image_blend, c_hover, 0.1);
	
}

else { 
	
	image_blend = merge_color(image_blend, c_default, 0.1);
	
}
*/

